{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile and run C",
            "type": "shell",
            "command": "g++ '${file}' -o ${relativeFileDirname}\\${fileBasenameNoExtension}.exe ; ./${relativeFileDirname}\\${fileBasenameNoExtension}.exe ",
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
